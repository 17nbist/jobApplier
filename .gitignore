# Secrets — OpenRouter API key
.env

# Design/reference docs — kept local, not published
docs/

# Test output — everything in artifacts/ (screenshots + fixture PDF) is
# regenerated by test/e2e*.mjs
test/artifacts/
node_modules/

# Extension packaging keys, if the extension is ever packed
*.pem
*.crx

# Claude Code local (per-machine) settings
.claude/settings.local.json

# OS noise
.DS_Store
